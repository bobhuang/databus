apply plugin: 'li-cmdline'

dependencies {
  compile project(':perf:databus-perf-core')
}

project.tasks.cmdline {

  into("conf") { from "conf/" }

  from("script") {
    into ("bin")
    fileMode = 0755
  }

}
