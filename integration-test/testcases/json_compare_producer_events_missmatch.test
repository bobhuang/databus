#!/bin/bash
#******************************************************
# set TEST_NAME before calling setup_env.inc
#******************************************************
export TEST_NAME=json_compare_producer_events_mismatch.test
#******************************************************
# sets up common environmnet variables and 
source setup_env.inc
# test json compare script for two similar files
#compare result
stat_txt="Test $0"
$SCRIPT_DIR/dbus2_json_compare.py $DATA_DIR/profile_relay_event_trace_json_extra $DATA_DIR/profile_consumer_json_valid.events 

source report_pass_fail.inc

# Verifying for negative test
if [ $all_stat == 1 ]; then
 echo Negative testcase
 exit 0  
fi


