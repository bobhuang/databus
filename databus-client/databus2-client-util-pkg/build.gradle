apply plugin: 'java'

dependencies {
  compile project(':databus-client:databus2-client-util')
}

project.tasks.cmdline {

  into("conf") { from "config/" }

  from("script") {
    into ("bin")
    fileMode = 0755
  }
}
